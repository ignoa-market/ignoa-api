spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ignoa_test
    username: root
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

  data:
    redis:
      host: localhost
      port: 6379

  mail:
    username: ci@example.com
    password: ci-dummy-password

cloud:
  aws:
    s3:
      bucket: ci-dummy-bucket
      region: ap-northeast-2

jwt:
  secret: ci-dummy-secret-key-for-tests-only-not-a-real-secret-value
  access-expiration: 3600000
  refresh-expiration: 604800000

oauth:
  kakao:
    client-id: ci-dummy-client-id
    client-secret: ci-dummy-client-secret
    redirect-uri: http://localhost:38080/api/auth/kakao/callback
